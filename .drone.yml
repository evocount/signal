kind: pipeline
name: default

steps:
  - name: install
    image: node
    commands:
      - npm i

  - name: test
    image: node
    commands:
      - npm test

  - name: build
    image: node
    commands:
      - npm run build:production

  - name: publish
    image: plugins/npm
    settings:
      username: arwed
      password:
        from_secret: npm_token
      email: arwed.mett@googlemail.com
